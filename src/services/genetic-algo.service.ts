import {Strategy} from "../models/strategy.model";

// Genetic algorithm will create closes and mutations of the best performing combinations of parameters
export class GeneticAlgoService {

    run(exchange, amount: number, tradingPair: string, interval: string, limit: number, strategies: Strategy[], commission: number) {


        // TODO: implement
        // https://en.wikipedia.org/wiki/Genetic_algorithm


    }
}
